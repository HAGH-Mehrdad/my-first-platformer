using UnityEngine;



// We should be aware of the fact that this enum is used in the animator blend tree, so if we change the values here, we should also update the blend tree accordingly.
public enum FruitType
{
    Apple,
    Banana,
    Cherry,
    Kiwi,
    Melon,
    Orange,
    Pineapple,
    Strawberry
}
